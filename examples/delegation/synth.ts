/**
 * Final-pass synthesizer. Real version: an Anthropic call that
 * folds successful subagent outputs into prose, ignoring failed peers.
 */

export function synthesize(_request: string, _jobs: unknown[]): string {
  throw new Error("not implemented");
}
