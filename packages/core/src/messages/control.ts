// Reserved module for ARCP control-plane envelopes. The protocol defines none;
// cancel is folded into `job.cancel` and resume into
// `session.hello.payload.resume`.

export const CONTROL_ENVELOPES = [] as const;
