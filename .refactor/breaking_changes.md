# Deferred Breaking Changes

Append-only list of public-API changes the refactor *would* make if
the guide were applied strictly, but that would break consumers and
therefore require explicit user approval before being applied.

Format per entry:

```
## <YYYY-MM-DD> — <package>:<symbol>

- Sub-phase: <which sub-phase surfaced this>
- Current shape: <description or signature>
- Proposed shape: <description or signature>
- Why it's breaking: <one line>
- Rationale for proposing the change: <one line>
- Status: deferred | approved | rejected
```

---

(none yet — Phase 1 surfaced no breaking changes)
